# POST /{make_id}/_api/{api_id}/_list/{list_id}
# application/json
# host: v2.cape.io

entities: # An array of entities that have changed.
  -
    _id: 91002-02
    sort: 1

# PUT /{make_id}/_api/{api_id}/_list/{list_id}/{entity_id}
# content-type: urlencoded form
# host: v2.cape.io

# sort={sort_value}&status={status_value}&otherfield=whatever
